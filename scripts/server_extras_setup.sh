#
# installing these packages can take a long time, so wait to do these until later
#

apt-get -y install emacs wget git-core mcrypt poppler-utils netpbm cups-pdf openoffice.org-writer openoffice.org-draw

aptitude -y full-upgrade