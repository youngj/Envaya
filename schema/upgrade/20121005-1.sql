ALTER TABLE translation_keys ADD KEY `lang_updated` (`language_guid`, `time_updated`);

ALTER table email_templates drop primary key, drop key `tid`, add primary key `tid` (`tid`), add unique key `guid` (`guid`), drop key `time_created`, add KEY `time_created` (`time_created`), drop key `time_updated`;
ALTER table sms_templates drop primary key, drop key `tid`, add primary key `tid` (`tid`), add unique key `guid` (`guid`), drop key `time_created`, add KEY `time_created` (`time_created`), drop key `time_updated`;
ALTER table discussion_messages drop primary key, drop key `tid`, add primary key `tid` (`tid`), add unique key `guid` (`guid`), drop key `time_created`, add KEY `time_created` (`time_created`), drop key `time_updated`;
ALTER table discussion_topics drop primary key, drop key `tid`, add primary key `tid` (`tid`), add unique key `guid` (`guid`), drop key `time_created`, add KEY `time_created` (`time_created`), drop key `time_updated`;
ALTER table featured_sites drop primary key, drop key `tid`, add primary key `tid` (`tid`), add unique key `guid` (`guid`), drop key `time_created`, add KEY `time_created` (`time_created`), drop key `time_updated`;
ALTER table featured_photos drop primary key, drop key `tid`, add primary key `tid` (`tid`), add unique key `guid` (`guid`), drop key `time_created`, add KEY `time_created` (`time_created`), drop key `time_updated`;
ALTER table org_relationships drop primary key, drop key `tid`, add primary key `tid` (`tid`), add unique key `guid` (`guid`), drop key `time_created`, add KEY `time_created` (`time_created`), drop key `time_updated`;
ALTER table report_definitions drop primary key, drop key `tid`, add primary key `tid` (`tid`), add unique key `guid` (`guid`), drop key `time_created`, add KEY `time_created` (`time_created`), drop key `time_updated`;
ALTER table reports drop primary key, drop key `tid`, add primary key `tid` (`tid`), add unique key `guid` (`guid`), drop key `time_created`, add KEY `time_created` (`time_created`), drop key `time_updated`;
ALTER table report_fields drop primary key, drop key `tid`, add primary key `tid` (`tid`), add unique key `guid` (`guid`), drop key `time_created`, add KEY `time_created` (`time_created`), drop key `time_updated`;
ALTER table report_comments drop primary key, drop key `tid`, add primary key `tid` (`tid`), add unique key `guid` (`guid`), drop key `time_created`, add KEY `time_created` (`time_created`), drop key `time_updated`;
ALTER table report_versions drop primary key, drop key `tid`, add primary key `tid` (`tid`), add unique key `guid` (`guid`), drop key `time_created`, add KEY `time_created` (`time_created`), drop key `time_updated`;
ALTER table translation_languages drop primary key, drop key `tid`, add primary key `tid` (`tid`), add unique key `guid` (`guid`), drop key `time_created`, add KEY `time_created` (`time_created`), drop key `time_updated`;
ALTER table interface_groups drop primary key, drop key `tid`, add primary key `tid` (`tid`), add unique key `guid` (`guid`), drop key `time_created`, add KEY `time_created` (`time_created`), drop key `time_updated`;
ALTER table translation_keys drop primary key, drop key `tid`, add primary key `tid` (`tid`), add unique key `guid` (`guid`), drop key `time_created`, add KEY `time_created` (`time_created`), drop key `time_updated`;
ALTER table translation_strings drop primary key, drop key `tid`, add primary key `tid` (`tid`), add unique key `guid` (`guid`), drop key `time_created`, add KEY `time_created` (`time_created`), drop key `time_updated`;
ALTER table translation_votes drop primary key, drop key `tid`, add primary key `tid` (`tid`), add unique key `guid` (`guid`), drop key `time_created`, add KEY `time_created` (`time_created`), drop key `time_updated`;
ALTER table translator_stats drop primary key, drop key `tid`, add primary key `tid` (`tid`), add unique key `guid` (`guid`), drop key `time_created`, add KEY `time_created` (`time_created`), drop key `time_updated`;
ALTER table translation_key_comments drop primary key, drop key `tid`, add primary key `tid` (`tid`), add unique key `guid` (`guid`), drop key `time_created`, add KEY `time_created` (`time_created`), drop key `time_updated`;
ALTER table files drop primary key, drop key `tid`, add primary key `tid` (`tid`), add unique key `guid` (`guid`), drop key `time_created`, add KEY `time_created` (`time_created`), drop key `time_updated`;
ALTER table comments drop primary key, drop key `tid`, add primary key `tid` (`tid`), add unique key `guid` (`guid`), drop key `time_created`, add KEY `time_created` (`time_created`), drop key `time_updated`;
ALTER table widgets drop primary key, drop key `tid`, add primary key `tid` (`tid`), add unique key `guid` (`guid`), drop key `time_created`, add KEY `time_created` (`time_created`), drop key `time_updated`, add KEY `time_updated` (`time_updated`);
ALTER table users drop primary key, drop key `tid`, add primary key `tid` (`tid`), add unique key `guid` (`guid`), drop key `time_created`, add KEY `time_created` (`time_created`), drop key `time_updated`;
ALTER table external_feeds drop primary key, drop key `tid`, add primary key `tid` (`tid`), add unique key `guid` (`guid`), drop key `time_created`, add KEY `time_created` (`time_created`), drop key `time_updated`;
ALTER table external_sites drop primary key, drop key `tid`, add primary key `tid` (`tid`), add unique key `guid` (`guid`), drop key `time_created`, add KEY `time_created` (`time_created`), drop key `time_updated`;
ALTER table sms_subscriptions drop primary key, drop key `tid`, add primary key `tid` (`tid`), add unique key `guid` (`guid`), drop key `time_created`, add KEY `time_created` (`time_created`), drop key `time_updated`;
ALTER table email_subscriptions drop primary key, drop key `tid`, add primary key `tid` (`tid`), add unique key `guid` (`guid`), drop key `time_created`, add KEY `time_created` (`time_created`), drop key `time_updated`;
ALTER table scheduled_events drop primary key, drop key `tid`, add primary key `tid` (`tid`), add unique key `guid` (`guid`), drop key `time_created`, add KEY `time_created` (`time_created`), drop key `time_updated`;
ALTER table user_scopes drop primary key, drop key `tid`, add primary key `tid` (`tid`), add unique key `guid` (`guid`), drop key `time_created`, add KEY `time_created` (`time_created`), drop key `time_updated`;
ALTER table permissions drop primary key, drop key `tid`, add primary key `tid` (`tid`), add unique key `guid` (`guid`), drop key `time_created`, add KEY `time_created` (`time_created`), drop key `time_updated`;